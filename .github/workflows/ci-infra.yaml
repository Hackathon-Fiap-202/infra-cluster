name: Terraform Plan (Infra Unificada)

on:
  pull_request:
    branches:
      - dev
      - hom
      - main
  push:
    branches:
      - feature/**

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}

jobs:
  plan-infra:
    name: Terraform Plan - Infraestrutura Completa
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/github-action-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format Check
        run: terraform fmt -check -recursive || true

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -var-file=terraform.tfvars

      - name: Plan Summary
        run: |
          echo "✅ Plan executado com sucesso!"
          echo "📦 Módulos incluídos:"
          echo "  - module.cluster (EKS)"
          echo "  - module.bootstrap_core (IRSA + Addons Core)"
          echo "  - module.bootstrap_addons (Datadog + Secrets)"
